<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.akashpratapsingh.attendance">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".predefine" />
        <activity android:name=".DateWize" />
        <activity android:name=".displaylist" />
        <activity android:name=".displaysubject" />
        <activity android:name=".Subjectdetail" />
        <activity android:name=".displaysubjectlist" />
        <activity android:name=".updateinfo"></activity>
    </application>

</manifest>